# Classroom Live locales

`en.json`을 복사하고 **값만** 번역하세요. 키와 `{name}`, `{count}`, `{line}` 같은 자리표시자는 바꾸지 않습니다.

- 파일명과 `$code`는 같은 소문자 언어 코드여야 합니다.
- `$name`은 언어 선택기에 표시할 언어의 자칭 이름입니다.
- `$direction`은 `ltr` 또는 `rtl`입니다.
- `ko.json`이 기준 키 목록입니다. 누락·추가 키와 자리표시자 불일치는 빌드 검증에서 실패합니다.

